# Only secrets and per-environment overrides belong here; everything else
# has a documented default in server.yaml.
GEOAPIFY_API_KEY=
TRAVEL_DATA_SOURCE=real
HTTP_USER_AGENT=local-travel-mcp/1.0 (local development)
MCP_HOST=127.0.0.1
MCP_PORT=8001
LOG_LEVEL=INFO
LOG_FILE=logs/travel-mcp.jsonl
OTEL_ENABLED=true
OTEL_SERVICE_NAME=travel-mcp
